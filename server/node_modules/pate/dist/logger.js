"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const chalk_1 = require("chalk");
class Logger {
    constructor(verbose = false) {
        this.log = (message, value = '', force = false) => {
            if (this.verbose || force) {
                console.log(chalk_1.default.grey(message), value);
            }
        };
        this.success = (message, value = '', force = false) => {
            if (this.verbose || force) {
                console.log(chalk_1.default.bgGreenBright.bold.black(`\n✔ ${message}`), `${value}\n`);
            }
        };
        this.warn = (message, value = '', force = false) => {
            if (this.verbose || force) {
                console.log(chalk_1.default.yellow(message), value);
            }
        };
        this.error = (message, value = '') => {
            console.log(chalk_1.default.bold.red(`\n✖ ${message}`), value);
        };
        this.verbose = verbose;
    }
}
exports.default = Logger;
