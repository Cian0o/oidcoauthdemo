import * as yargs from 'yargs';
export interface IArguments extends yargs.Arguments {
    glob: string;
    ignore: string[];
    output: string;
    searchPath: string;
    verbose: boolean;
}
