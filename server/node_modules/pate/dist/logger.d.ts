export declare type loggerT = (message: string, value?: any, force?: boolean) => void;
declare class Logger {
    private verbose;
    constructor(verbose?: boolean);
    log: loggerT;
    success: loggerT;
    warn: loggerT;
    error: loggerT;
}
export default Logger;
