import { IOptions, IProcessFactories, discoverFactories, findMatchInFile, indexFiles, processFactories } from './core';
export { discoverFactories, findMatchInFile, indexFiles, processFactories };
export declare type pateT = (matchPattern: RegExp, searchPath: string, options: IOptions) => Promise<IProcessFactories>;
declare const pate: pateT;
export default pate;
