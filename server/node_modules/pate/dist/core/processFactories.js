"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const utils_1 = require("../utils");
const processFactories = async (factories) => {
    const rawResult = await utils_1.serializePromise(factories);
    const matchedFiles = rawResult.reduce((acc, { result, skipped }) => {
        if (!skipped && !!result) {
            acc.push(result);
        }
        return acc;
    }, []);
    const skippedFiles = rawResult.reduce((acc, { result, skipped }) => {
        if (skipped) {
            acc.push(result);
        }
        return acc;
    }, []);
    return {
        matchedFiles,
        skippedFiles,
    };
};
exports.default = processFactories;
