"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const _1 = require("./");
const discoverFactories = (files, regexPattern, bar) => files.map((file) => async () => {
    try {
        if (bar) {
            bar.tick();
        }
        return {
            result: await _1.findMatchInFile(regexPattern, file),
            skipped: false,
        };
    }
    catch (error) {
        return {
            result: file,
            skipped: true,
        };
    }
});
exports.default = discoverFactories;
